import { FC, useEffect, useState } from "react";
import styles from "../styles/WelcomeText.module.css";

export const WelcomeText: FC = () => {
    const [text, setText] = useState<string[]>([]);

    useEffect(() => {
        setText([
            "Добро пожаловать в [Название игры] – битву стратегии, силы и риска!",
            "",
            "Ты вступаешь в игру, где две команды сражаются за призовой фонд. Каждый участник делает вклад, но не все заберут награду.",
            "🔹 Выбери свою стратегию: вкладывай больше, чтобы претендовать на крупный куш, или полагайся на мастерство и набирай очки.",
            "🔹 Помни о риске: проигравшие теряют всё, а победители получают свои вклады обратно + бонус по вкладу в победу!",
            "🔹 Зарабатывай очки: чем больше твой вклад в команду, тем больше твоя награда.",
            "",
            "Сможешь ли ты превратить свой вклад в настоящий выигрыш? Время узнать. Присоединяйся и начни свою игру!"
        ]);
    }, []);

    if (!text.length) return null; // Пока текст загружается — ничего не рендерим

    return (
        <div className={styles.welcome}>
            {text.map((line, index) => (
                <p key={index}>{line}</p>
            ))}
        </div>
    );
};
